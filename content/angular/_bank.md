---
stack: angular
kind: question-bank
source: curated
sourceUrl: https://angular.dev
license: curated
copyright: Written from scratch citing official documentation
---

## Interview Questions

### ¿Cuál es la diferencia entre `@Component`, `@Directive` y `@Pipe` en Angular?
Los tres son decoradores que marcan una clase como metadato Angular, pero tienen propósitos distintos. `@Component` define una vista propia con template y estilos; siempre tiene un selector de elemento. `@Directive` no tiene template: modifica el comportamiento o aspecto de un elemento existente en el DOM. `@Pipe` transforma valores en templates sin lógica de UI. En la práctica, si necesitás renderizar HTML propio, es un componente; si solo querés cambiar algo en un elemento existente, es una directiva; si querés transformar un valor para mostrarlo, es un pipe. [angular.dev/guide/components](https://angular.dev/guide/components)

### ¿Cuándo usarías `@Input` vs un servicio compartido para pasar datos entre componentes?
`@Input` es apropiado cuando los componentes están en una relación directa padre-hijo y el dato es parte de la interfaz pública del componente hijo. Un servicio compartido (o un signal en un servicio) es la solución correcta cuando los componentes no tienen relación directa en el árbol (siblings, componentes en módulos distintos), o cuando múltiples partes de la app necesitan el mismo estado. Mezclar ambos en el mismo flujo crea confusión: el estado queda duplicado y divergente. La regla simple: si tenés que pasar un dato más de dos niveles hacia abajo en el árbol, considerá sacarlo a un servicio. [angular.dev/guide/components/inputs](https://angular.dev/guide/components/inputs)

### Explicá el orden completo de los lifecycle hooks de un componente Angular.
El orden es: `ngOnChanges` → `ngOnInit` → `ngDoCheck` → `ngAfterContentInit` → `ngAfterContentChecked` → `ngAfterViewInit` → `ngAfterViewChecked` → `ngOnDestroy`. `ngOnChanges` se ejecuta antes de `ngOnInit` si el componente tiene inputs, y luego en cada cambio de input. `ngOnInit` se ejecuta una sola vez, después del primer `ngOnChanges`. `ngAfterViewInit` garantiza que los `@ViewChild` están disponibles. El par `ngAfterContentChecked` / `ngAfterViewChecked` se ejecuta en cada ciclo de change detection, lo que los hace costosos si se abusa de ellos. [angular.dev/guide/components/lifecycle](https://angular.dev/guide/components/lifecycle)

### ¿Por qué el constructor de un componente no es el lugar para llamar servicios HTTP o acceder a @Input?
El constructor existe para inyectar dependencias via el sistema de DI, no para inicializar lógica del componente. En el momento en que se ejecuta el constructor, los inputs aún no tienen sus valores (Angular los asigna después, en la primera ejecución de `ngOnChanges`). Las llamadas HTTP o cualquier inicialización que dependa de los inputs deben ir en `ngOnInit`, donde los inputs ya están disponibles. Usar el constructor para lógica de inicialización también dificulta el testing, porque no podés controlar el momento de la inicialización en los tests. [angular.dev/guide/components/lifecycle](https://angular.dev/guide/components/lifecycle)

### ¿Qué es `ChangeDetectionStrategy.OnPush` y cuándo deberías usarlo?
Con `OnPush`, Angular solo ejecuta change detection en un componente cuando: un `@Input` cambia por referencia, ocurre un evento en el template del componente, se llama `markForCheck()` explícitamente, o se usa `AsyncPipe` (que llama `markForCheck()` internamente). Es la optimización más impactante en componentes presentacionales que reciben datos del exterior: evitás que toda la app recompute cuando algo cambia en otra rama del árbol. El tradeoff es que mutaciones de objetos existentes no se detectan, lo que te fuerza a usar patrones inmutables (crear nuevas referencias). Lo recomendado es usarlo por defecto en componentes que reciben solo `@Input` y no tienen estado interno complejo. [angular.dev/best-practices/skipping-component-subtrees](https://angular.dev/best-practices/skipping-component-subtrees)

### ¿Cuál es la diferencia entre una directiva estructural y una de atributo?
Las directivas estructurales modifican la estructura del DOM: agregan, eliminan o repiten elementos HTML. Se reconocen por el prefijo `*` (`*ngIf`, `*ngFor`, `*ngSwitch`), que Angular desenvuelve en un `<ng-template>`. Las directivas de atributo no cambian la estructura: actúan sobre un elemento ya existente para cambiar su apariencia o comportamiento (clases CSS, estilos, eventos). `NgClass` y `NgStyle` son ejemplos built-in de atributo. Para crear una directiva de atributo propia se usa `@Directive({ selector: '[appMiDirectiva]' })`. La distinción es importante porque determina cómo se aplica al template y qué APIs están disponibles. [angular.dev/guide/directives](https://angular.dev/guide/directives)

### ¿Qué son los Angular Signals y en qué se diferencian de BehaviorSubject?
Un `signal()` es una primitiva reactiva nativa de Angular que almacena un valor y notifica automáticamente a sus consumidores cuando cambia. A diferencia de `BehaviorSubject`, no requiere suscripciones manuales, no puede producir memory leaks por suscripciones no canceladas, y su integración con el sistema de change detection de Angular es granular: solo se actualizan los componentes que leen ese signal, sin necesidad de zone.js. `BehaviorSubject` sigue siendo la herramienta correcta para streams asíncronos complejos donde aprovechás los operadores de RxJS; Signal es más apropiado para estado local de componente o estado global sincrónico. [angular.dev/guide/signals](https://angular.dev/guide/signals)

### ¿Cómo funciona `computed()` en Angular Signals y cuándo se recalcula?
`computed()` crea un signal derivado cuyo valor se calcula a partir de otros signals. Es **lazy**: no se calcula hasta que algo lo lee. Es **memoizado**: si las dependencias no cambiaron desde la última lectura, retorna el valor cacheado sin recalcular. Las dependencias se detectan automáticamente en la primera ejecución: Angular rastrea qué signals se leyeron durante el cálculo y los registra como dependencias. Si cualquiera de esas dependencias cambia, el computed se marca como dirty y se recalculará en la próxima lectura. Un computed no puede tener side-effects; para eso existe `effect()`. [angular.dev/guide/signals#computed](https://angular.dev/guide/signals)

### ¿Qué problema resuelve `effect()` en Signals y qué restricciones tiene?
`effect()` ejecuta una función reactiva cada vez que alguno de los signals que lee cambia. Se usa para sincronizar el estado de Angular con el mundo exterior: escribir en localStorage, actualizar una librería externa, loggear cambios. La restricción principal es que no se puede escribir a un signal dentro de un `effect()` por defecto (causa un ciclo infinito); si se necesita, hay que pasar `{ allowSignalWrites: true }`. Los effects se limpian automáticamente cuando el contexto de inyección que los creó se destruye, o se pueden destruir manualmente via `effect.destroy()`. Deben crearse dentro de un injection context (constructor, campo de clase, o via `runInInjectionContext`). [angular.dev/guide/signals#effects](https://angular.dev/guide/signals)

### ¿Cómo convierte toSignal() un Observable en un Signal y qué hace con errores?
`toSignal()` suscribe al Observable inmediatamente dentro del injection context y expone su último valor emitido como un signal de solo lectura. Si el Observable no ha emitido todavía, el signal retorna `undefined` (o el valor de `initialValue` si se provee). Si el Observable lanza un error, ese error se re-lanza la próxima vez que algo lee el signal. La suscripción se cancela automáticamente cuando el contexto de inyección se destruye, lo que evita memory leaks sin `takeUntilDestroyed`. Para el caso inverso, `toObservable()` convierte un signal en un Observable, útil para componer con operadores RxJS como `debounceTime` o `filter`. [angular.dev/ecosystem/rxjs-interop](https://angular.dev/ecosystem/rxjs-interop)

### ¿Cuál es la diferencia entre Observable y Promise en Angular?
Un Observable es lazy (no ejecuta hasta que alguien se suscribe), puede emitir múltiples valores a lo largo del tiempo, y puede cancelarse via `unsubscribe()`. Una Promise es eager (se ejecuta al crearse), emite exactamente un valor (o error), y no puede cancelarse. En Angular, `HttpClient` retorna Observables porque permite cancelar requests HTTP en vuelo cuando el componente se destruye o el usuario navega a otra ruta. Los Observables también habilitan composición con operadores: filtrar, transformar, combinar streams antes de que el componente los consuma. Convertir un Observable a Promise con `firstValueFrom()` es válido cuando solo esperás un valor y no necesitás cancelación. [angular.dev/guide/http](https://angular.dev/guide/http)

### ¿Cuándo usarías switchMap, mergeMap, concatMap o exhaustMap?
Cada uno trata de forma diferente qué pasa cuando llega un nuevo valor mientras el Observable interior aún no terminó. `switchMap` cancela el Observable anterior y empieza uno nuevo, ideal para búsquedas en tiempo real donde el resultado obsoleto no interesa. `mergeMap` ejecuta todos en paralelo sin cancelar, ideal para requests independientes donde el orden no importa. `concatMap` espera que el actual termine antes de empezar el siguiente, ideal para operaciones que deben ejecutarse en orden (eventos de auditoría, cola de mensajes). `exhaustMap` ignora nuevos valores mientras el actual no terminó, ideal para evitar doble submit de formularios. Confundirlos genera bugs difíciles: por ejemplo, usar `mergeMap` para un login puede procesar dos logins en paralelo con resultados impredecibles. [angular.dev/ecosystem/rxjs-interop](https://angular.dev/ecosystem/rxjs-interop)

### ¿Por qué AsyncPipe es preferido sobre suscripciones manuales en componentes?
`AsyncPipe` se suscribe al Observable (o Signal, o Promise) cuando el componente se inicializa y se desuscribe automáticamente cuando el componente se destruye, eliminando la fuente principal de memory leaks en Angular. Además, cuando el Observable emite un nuevo valor, `AsyncPipe` llama internamente `markForCheck()`, lo que hace que funcione correctamente con `ChangeDetectionStrategy.OnPush` sin intervención manual. El código también queda más limpio: en lugar de gestionar una propiedad, una suscripción y cleanup en `ngOnDestroy`, el template expresa la dependencia declarativamente. La única limitación es que no funciona bien cuando el mismo Observable se referencia múltiples veces en el template (crea múltiples suscripciones); en ese caso se usa el patrón `*ngIf="data$ | async as data"`. [angular.dev/api/common/AsyncPipe](https://angular.dev/api/common/AsyncPipe)

### ¿Qué es lazy loading en Angular y cómo se implementa con componentes standalone?
Lazy loading difiere la descarga de partes de la app hasta que el usuario navegue a ellas, reduciendo el bundle inicial. Con componentes standalone, la forma moderna es usar `loadComponent` en la configuración de rutas: `{ path: 'reports', loadComponent: () => import('./reports/reports.component').then(m => m.ReportsComponent) }`. Angular genera un chunk separado para ese componente (y sus importaciones directas) que solo se descarga al navegar a esa ruta. Para grupos de rutas, `loadChildren` retorna un array de rutas en un archivo separado: `loadChildren: () => import('./admin/admin.routes').then(m => m.ADMIN_ROUTES)`. Una estrategia de lazy loading bien diseñada puede reducir el LCP significativamente en apps grandes. [angular.dev/guide/ngmodules/lazy-loading](https://angular.dev/guide/ngmodules/lazy-loading)

### ¿Cómo funcionan los route guards y cuál es la forma moderna de implementarlos?
Los route guards son funciones (o clases) que Angular ejecuta antes de activar, cargar, o desactivar una ruta. Retornan `true` para permitir la navegación, `false` para bloquearla, o un `UrlTree` para redirigir. Desde Angular 15.2+, la forma recomendada es como funciones puras usando `inject()`: `export const authGuard = () => { const auth = inject(AuthService); return auth.isLoggedIn() ? true : inject(Router).createUrlTree(['/login']); }`. `canActivate` protege el acceso a una ruta. `canDeactivate` pregunta al usuario antes de abandonar (útil para formularios con cambios sin guardar). `canMatch` decide si una ruta se considera para matching, lo que permite rutas diferentes para usuarios con distintos roles. [angular.dev/guide/routing/common-router-tasks](https://angular.dev/guide/routing/common-router-tasks)

### ¿Cuál es la diferencia entre formularios template-driven y reactivos? ¿Cuándo elegís uno?
Los formularios template-driven definen el modelo en el HTML via directivas (`ngModel`, `required`); son simples y apropiados para formularios pequeños con validación estándar. Los reactive forms definen el modelo en TypeScript con `FormControl`, `FormGroup`, y `FormArray`; son más explícitos, predecibles, y testeables porque el estado del formulario es un objeto JS que podés manipular directamente sin tocar el DOM. Elegís reactive cuando el formulario tiene validaciones dinámicas, campos que se agregan/eliminan programáticamente (`FormArray`), o validadores asíncronos. Elegís template-driven cuando la velocidad de desarrollo importa más que el control fino y el formulario es simple. Mezclar ambos en la misma app está bien; mezclarlos en el mismo formulario no. [angular.dev/guide/forms](https://angular.dev/guide/forms)

### ¿Cómo implementás un validator asíncrono para verificar que un email no existe en el servidor?
Un validator asíncrono es una función que recibe un `AbstractControl` y retorna un `Observable<ValidationErrors | null>` o `Promise<ValidationErrors | null>`. Retorna `null` si el control es válido, o un objeto de error si no: `{ emailTaken: true }`. Se agrega al control en el campo `asyncValidators` del config object de `FormControl` o `FormBuilder`. El estado del control será `pending` mientras el Observable no complete. Para evitar un request al servidor en cada tecla, se agrega `updateOn: 'blur'` para que el validator solo se ejecute cuando el usuario sale del campo. Si el validator síncono ya falló (`Validators.email`), Angular no ejecuta los asíncronos, ahorrando requests innecesarios. [angular.dev/guide/forms/form-validation#async-validation](https://angular.dev/guide/forms/form-validation)

### ¿Qué es FormArray y cuándo lo usarías?
`FormArray` es un contenedor de controles indexados numéricamente, útil para listas dinámicas de inputs donde el usuario puede agregar o eliminar entradas: tags, teléfonos, links, ítems de una factura. A diferencia de `FormGroup` (donde cada control tiene una clave string), `FormArray` mantiene un array de controles que se puede manipular con `push()`, `removeAt()`, e `insert()`. El estado de validez del FormArray es el AND de todos sus controles internos. En el template se accede via `get('myArray') as FormArray` y se itera con `@for (ctrl of myArray.controls; track $index)`. Es el reemplazo correcto del antipatrón de usar múltiples controls numerados manualmente (`phone_0`, `phone_1`). [angular.dev/guide/forms/reactive-forms#creating-dynamic-form-controls](https://angular.dev/guide/forms/reactive-forms)

### ¿Cómo funciona la jerarquía de injectors en Angular y cuándo importa?
Angular tiene un árbol de injectors que refleja el árbol de componentes. El injector raíz (root) provee servicios disponibles en toda la app. Cada componente puede tener su propio injector si declara `providers` en su `@Component`. Cuando Angular necesita resolver un token, busca hacia arriba en el árbol: primero el injector del componente actual, luego el del padre, y así hasta el root. Esto importa cuando querés **estado aislado por instancia**: si dos instancias del mismo componente no deben compartir el mismo servicio, proveés el servicio en el componente (no en root). Un caso concreto: un `FormService` o `WizardStateService` que debe ser independiente para cada instancia de un formulario multi-paso. [angular.dev/guide/di/hierarchical-dependency-injection](https://angular.dev/guide/di/hierarchical-dependency-injection)

### ¿Para qué sirve InjectionToken y cuándo lo usás en vez de inyectar una clase?
`InjectionToken` permite inyectar valores que no son clases: strings, números, objetos de configuración, o implementaciones de interfaces (que no existen en runtime en TypeScript). Se crea fuera de la clase con `new InjectionToken<T>('descripcion')` y se provee en el módulo o `bootstrapApplication` con `{ provide: TOKEN, useValue: valor }`. Se inyecta via `inject(TOKEN)` o `@Inject(TOKEN)` en el constructor. El caso de uso más común es inyectar configuración de entorno (`API_URL`, `FEATURE_FLAGS`) que varía entre ambientes, de forma que los servicios no importen directamente los archivos de environment y el testing sea más simple. [angular.dev/api/core/InjectionToken](https://angular.dev/api/core/InjectionToken)

### ¿Qué hace trackBy (o track) en ngFor y por qué es importante?
Sin `track`, cuando Angular detecta que el array cambió, destruye y recrea todos los DOM nodes de la lista. Con `track item.id`, Angular puede identificar qué items cambiaron, cuáles son nuevos, y cuáles se eliminaron, y solo modifica los nodos necesarios. Esto es crítico en listas que se actualizan frecuentemente (resultados de búsqueda, feeds en tiempo real): sin track, el DOM se regenera completo en cada actualización, lo que destruye el estado de inputs en la lista, fuerza reflows/repaints innecesarios, y degrada la performance. Con la sintaxis moderna `@for`, `track` es obligatorio y el compilador de Angular lo exige explícitamente. [angular.dev/guide/templates/control-flow#track-in-for-blocks](https://angular.dev/guide/templates/control-flow)

### ¿Qué es un pipe puro vs impuro en Angular? ¿Cuándo usarías uno impuro?
Un pipe **puro** (el default) solo se re-ejecuta cuando la referencia del valor de entrada cambia. Es eficiente porque Angular puede cachear el resultado. Un pipe **impuro** (`pure: false`) se re-ejecuta en cada ciclo de change detection, incluso si el input no cambió. Casi nunca querés un pipe impuro: es costoso y normalmente indica que se está usando un pipe para algo que debería ser un componente o una computed property en el componente. El caso legítimo más común es `AsyncPipe`, que internamente es impuro porque necesita verificar el Observable en cada ciclo. Si necesitás filtrar un array mutable y querés que el pipe lo refleje, la solución correcta es crear un nuevo array con los filtros aplicados (inmutabilidad) y usar un pipe puro. [angular.dev/guide/pipes/pure-and-impure-pipes](https://angular.dev/guide/pipes)

### ¿Cómo previene Angular los ataques XSS en templates?
Angular sanitiza automáticamente los valores que se insertan en el DOM via binding. Si se intenta bindear HTML arbitrario en un property binding normal, Angular lo escapa. Para insertar HTML dinámico genuinamente confiable, se debe usar `[innerHTML]` con un valor marcado explícitamente como seguro via `DomSanitizer.bypassSecurityTrustHtml()`. La regla práctica es nunca llamar `bypassSecurity*` con datos que vengan del usuario o de una API externa sin sanitizar. Angular también desactiva CSS, URLs, y scripts potencialmente peligrosos por defecto. El compilador de templates previene ataques de template injection en tiempo de compilación, lo que es una ventaja de AOT. [angular.dev/best-practices/security](https://angular.dev/best-practices/security)

### ¿Qué es un HTTP Interceptor y para qué casos de uso es ideal?
Un interceptor es una función (o clase con `HttpInterceptor`) que Angular ejecuta en cada request o response HTTP. Permite centralizar lógica transversal sin repetirla en cada llamada al servicio: agregar headers de autorización (token JWT), loggear requests, manejar errores globalmente (redirigir a login en 401), mostrar un loading global, o reintentar requests fallidos. Con la API funcional de Angular 15+ (`HttpInterceptorFn`), un interceptor es una función pura que recibe la request y el `next` handler: `export const authInterceptor: HttpInterceptorFn = (req, next) => { const token = inject(AuthService).token(); return next(req.clone({ setHeaders: { Authorization: \`Bearer ${token}\` } })); }`. Es mucho más simple que la forma de clase y fácil de testear. [angular.dev/guide/http/interceptors](https://angular.dev/guide/http/interceptors)

### ¿Cómo funciona la compilación AOT en Angular y qué ventajas tiene sobre JIT?
AOT (Ahead-of-Time) compila los templates Angular a JavaScript durante el build, no en el navegador. JIT (Just-in-Time) compila en el browser al cargar la app. Angular usa AOT por defecto desde Angular 9 con Ivy. Las ventajas de AOT son: bundle inicial más pequeño (el compilador no se incluye), errores de template detectados en build time (no en runtime), mejor rendimiento de arranque porque el browser no necesita compilar, y mejor seguridad porque los templates se compilan a código que Angular puede analizar estáticamente. En la práctica, el único caso donde JIT era relevante era en development para rebuilds más rápidos, pero el compilador incremental de Ivy hace que AOT también sea rápido en dev. [angular.dev/tools/cli/aot-compiler](https://angular.dev/tools/cli/aot-compiler)

### ¿Cuál es la diferencia entre standalone: true y NgModule? ¿Cuándo migrarías?
Un componente con `standalone: true` declara sus propias dependencias en el `imports` del decorador y no necesita ser declarado en ningún `NgModule`. Un `NgModule` es un contenedor que agrupa componentes, directivas, y pipes, y gestiona qué se exporta para que otros módulos lo puedan usar. Standalone simplifica el modelo mental: cada componente es autocontenido, el árbol de dependencias es explícito, y el tree-shaking funciona mejor. Migrarías a standalone en proyectos nuevos (recomendado por defecto en Angular 17+) y en proyectos existentes cuando el costo de migración es aceptable. `importProvidersFrom()` y la coexistencia de módulos standalone con NgModules permiten una migración gradual sin reescribir todo. [angular.dev/guide/components/importing](https://angular.dev/guide/components/importing)

### ¿Qué es `bootstrapApplication()` y cómo difiere de `platformBrowserDynamic().bootstrapModule()`?
`bootstrapApplication()` es el punto de entrada para apps Angular standalone (sin AppModule). Recibe el componente raíz y un objeto de configuración con `providers`. `platformBrowserDynamic().bootstrapModule(AppModule)` es el bootstrap basado en NgModule que requiere un módulo raíz. La diferencia práctica es que con `bootstrapApplication`, toda la configuración global (router, HTTP client, animaciones) se provee via funciones específicas (`provideRouter()`, `provideHttpClient()`) en lugar de importar NgModules. Esto hace la configuración más explícita, más tree-shakeable, y más fácil de testear porque los providers se pueden sobreescribir en tests sin armar un módulo completo. [angular.dev/api/platform-browser/bootstrapApplication](https://angular.dev/api/platform-browser/bootstrapApplication)

### ¿Cómo funciona la detección de cambios con zone.js y cuál es la alternativa zoneless?
Zone.js parchea todas las APIs asíncronas del navegador (setTimeout, fetch, addEventListener) e intercepta su ejecución para notificar a Angular. Cada vez que cualquier operación asíncrona termina, Angular asume que algo pudo haber cambiado y ejecuta change detection desde la raíz. Esto es cómodo pero ineficiente: un WebSocket con alta frecuencia puede disparar cientos de ciclos de CD por segundo. La alternativa **zoneless** (experimental en Angular 17, evolucionando en v18+) no usa zone.js: Angular solo actualiza lo que está conectado a Signals que cambiaron. Para activar el modo experimental: `bootstrapApplication(App, { providers: [provideExperimentalZonelessChangeDetection()] })`. La recomendación actual es adoptar Signals progresivamente y prepararse para la migración zoneless. [angular.dev/guide/experimental/zoneless](https://angular.dev/guide/experimental/zoneless)

### ¿Cómo configurarías rutas hijas (children) y en qué caso las usarías?
Las rutas hijas se definen con la propiedad `children` en una ruta padre y requieren un `<router-outlet>` en el template del componente padre donde se renderizan. Se usan cuando una sección de la app tiene su propio sub-navegación con un layout compartido: un dashboard con una barra lateral que permanece, y el contenido central cambia según la sub-ruta. También se usan para representar jerarquías de datos: `/products/:id/reviews`. El componente padre actúa como shell (layout) y los hijos proveen el contenido. Sin `children`, habría que repetir el layout en cada componente. Las rutas hijas también heredan los guards del padre, lo que permite proteger todo un subtree con un solo `canActivate`. [angular.dev/guide/routing/router-tutorial-toh#child-routing-component](https://angular.dev/guide/routing)

### ¿Qué es NgZone.runOutsideAngular() y cuándo lo usarías?
`NgZone.runOutsideAngular()` ejecuta código fuera de la zona de Angular, de modo que las operaciones asíncronas dentro (timers, eventos) no disparan change detection. Se usa cuando tenés código de alta frecuencia que actualiza algo que no está directamente en la UI de Angular: bucles de animación con `requestAnimationFrame`, WebSockets con datos de alta frecuencia, librerías de mapas o canvas. Cuando finalmente querés actualizar la UI, llamás `ngZone.run(() => { this.data = newValue; })` para entrar a la zona y disparar CD solo cuando realmente hay algo que mostrar. Sin esto, una animación de 60fps dispararía change detection 60 veces por segundo en toda la app. [angular.dev/api/core/NgZone](https://angular.dev/api/core/NgZone)

### ¿Qué devuelve ActivatedRoute y cómo accedés a parámetros de ruta de forma reactiva?
`ActivatedRoute` provee información sobre la ruta activa: parámetros de ruta, query params, data del resolver, y el estado del router. Para acceder a parámetros de forma reactiva (cuando el componente puede reutilizarse sin destruirse, por ejemplo navegando entre `/users/1` y `/users/2`), se usa el Observable `route.paramMap.pipe(switchMap(params => this.service.getUser(params.get('id'))))`. Para casos donde el componente siempre se destruye y recrea al cambiar la ruta (lo más común), `route.snapshot.paramMap.get('id')` es suficiente y más simple. Los query params se acceden con `route.queryParamMap` o `route.snapshot.queryParamMap`. Con Angular 16+, `inject(ActivatedRoute)` fuera del constructor también funciona. [angular.dev/api/router/ActivatedRoute](https://angular.dev/api/router/ActivatedRoute)

### ¿Cómo testearías un componente Angular que usa servicios y hace llamadas HTTP?
La estrategia es aislar el componente de sus dependencias reales. Se usa `TestBed.configureTestingModule()` para configurar el entorno de test e inyectar mocks de los servicios via `providers: [{ provide: MyService, useValue: mockService }]`. Para HTTP específicamente, Angular provee `HttpClientTestingModule` y `HttpTestingController` que permiten interceptar requests en el test y responder con datos controlados. Para servicios con Signals o BehaviorSubject, se crea un spy con `jasmine.createSpyObj` o se provee un stub simple. El patrón AAA (Arrange, Act, Assert): setup del mock → acción en el componente → verificar que el DOM o el estado cambió como esperado. `fixture.detectChanges()` dispara el ciclo de CD manual en los tests. [angular.dev/guide/testing/components-scenarios](https://angular.dev/guide/testing)

### ¿Cuál es la diferencia entre providedIn: 'root' y provided en el componente?
`providedIn: 'root'` registra el servicio en el injector raíz de la app, creando un **singleton** compartido por toda la aplicación. Todos los componentes que inyectan ese servicio reciben la misma instancia. Proveer en el componente (`providers: [MyService]` en el decorador `@Component`) crea una instancia nueva por cada instancia del componente, independiente del resto de la app. Esta instancia también se destruye cuando el componente se destruye. La elección importa cuando el servicio tiene estado: `AuthService` y `HttpClient` claramente deben ser singletons; un `FormWizardService` que mantiene el estado de un formulario multi-paso debería ser scoped al componente que lo contiene para que múltiples instancias del wizard tengan su propio estado. [angular.dev/guide/di/hierarchical-dependency-injection](https://angular.dev/guide/di/hierarchical-dependency-injection)

### ¿Qué es Resolver en Angular Router y cuándo deberías usarlo?
Un Resolver es una función (o clase) que Angular ejecuta antes de activar la ruta y retorna datos que se inyectan al componente via `ActivatedRoute.data`. Se usa cuando el componente necesita datos para renderizar su estado inicial y querés evitar el pattern "spinner vacío → datos llegan → UI jumps". Con un resolver, Angular no activa la ruta hasta que los datos están disponibles, y el componente siempre arranca con datos. El tradeoff: la navegación se "pausa" visualmente mientras el resolver carga, lo que puede sentirse lento. Para datos no críticos (que pueden llegar después), es mejor cargarlos en `ngOnInit` y manejar el loading state en el componente. La forma moderna es `ResolveFn<T>` funcional con `inject()`. [angular.dev/api/router/Resolve](https://angular.dev/api/router/Resolve)

### ¿Cómo manejarías memoria y limpieza de recursos en un componente Angular?
La regla base es: todo lo que creás (suscripciones, timers, event listeners manuales) tenés que limpiarlo en `ngOnDestroy`. Para suscripciones a Observables, las opciones modernas son: `AsyncPipe` en el template (se desuscribe sola), `takeUntilDestroyed(this.destroyRef)` en `ngOnInit`, o guardar la suscripción en una propiedad y llamar `this.sub?.unsubscribe()` en `ngOnDestroy`. Para signals con `effect()`, el cleanup es automático. Para event listeners manuales del DOM o de librerías externas, `ngOnDestroy` es el único lugar. En components con `ChangeDetectionStrategy.OnPush`, también hay que llamar `this.cdr.detach()` en `ngOnDestroy` si usás change detection manual para evitar memory leaks en el detector. [angular.dev/guide/components/lifecycle#ngondestroy](https://angular.dev/guide/components/lifecycle)

### ¿Qué es Content Projection en Angular y cómo funciona ng-content?
Content projection permite que un componente defina "slots" en su template donde el padre puede inyectar HTML arbitrario. `<ng-content>` marca el slot donde va el contenido proyectado. Con `select`, se puede hacer multi-slot projection: `<ng-content select="[slot-header]">` captura solo los elementos del padre que tengan el atributo `slot-header`. Es el mecanismo central para componentes reutilizables de UI (modales, cards, tabs, acordeones) donde el layout es fijo pero el contenido varía. `@ContentChild` y `@ContentChildren` permiten acceder a los elementos proyectados desde el código del componente, disponibles desde `ngAfterContentInit`. Sin content projection, se acabaría usando `@Input` con templates, lo cual es mucho más verboso. [angular.dev/guide/components/content-projection](https://angular.dev/guide/components/content-projection)

### ¿Qué son los Signals en el contexto de rendimiento y cómo habilitan la arquitectura zoneless?
Con Signals, Angular puede construir un grafo de dependencias preciso: sabe exactamente qué componentes dependen de qué signals. Cuando un signal cambia, Angular puede actualizar quirúrgicamente solo los componentes afectados, sin recorrer todo el árbol de componentes como hace con zone.js. Esto habilita la arquitectura **zoneless**: la app no necesita zone.js porque Angular nunca necesita "adivinar" qué cambió. El resultado es un arranque más rápido (sin el overhead de parchear APIs globales), mejor SSR, y mejor compatibility con Web Workers. La migración hacia zoneless no requiere reescribir todo a la vez: Signals y zone.js pueden coexistir, y cada componente migrado a Signals reduce el trabajo de zone.js. [angular.dev/guide/experimental/zoneless](https://angular.dev/guide/experimental/zoneless)
